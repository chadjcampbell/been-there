import { CommentType } from "../../routes/Home";

type CommentCardProps = {
  comment: CommentType;
};

const CommentCard = ({ comment }: CommentCardProps) => {
  return <div>comments</div>;
};

export default CommentCard;
